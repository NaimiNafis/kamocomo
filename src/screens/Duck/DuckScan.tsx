/** `/duck/scan?spot=<qr_token>` geofenced stamp scan (Appendix A.2b) — built in Phase 8. */
export function DuckScan() {
  return <div className="p-6 font-ui text-kamo-ink">Duck scan — coming in Phase 8.</div>;
}
