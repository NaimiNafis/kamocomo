# Copy to .env.local (gitignored) and fill in real values.
# See BUILD_PLAN.md's "before Phase 1" steps for how to obtain each one.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_GOOGLE_MAPS_API_KEY=
# Optional — only for the graphical map WITHOUT labels. See README.
VITE_GOOGLE_MAPS_LABEL_FREE_MAP_ID=
